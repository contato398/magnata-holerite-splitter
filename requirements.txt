flask==3.0.3
pdfplumber==0.11.4
pypdf==4.3.1
gunicorn==22.0.0
requests==2.32.3
fpdf2==2.7.9
reportlab==4.2.2
xlrd==1.2.0
openpyxl==3.1.5
celery==5.3.6
redis==5.0.1
psycopg[binary]==3.2.*

# Cliente Gmail readonly. O adapter importa estas dependencias apenas nas
# funcoes do recurso real; testes e Captura Shadow V1 usam duplos sem rede.
google-api-python-client==2.149.0
google-auth==2.35.0

# Cliente S3 real para a composicao de producao do Orquestrador
# (magnata_os/orquestrador/ciclo_producao_v1.py). O adapter de storage
# (magnata_os/documental/modulo01/adapters/s3_armazenamento.py) e' duck-
# typed e nunca importa boto3 diretamente -- so a camada de composicao/
# infra (fora do dominio) instancia boto3.client('s3', ...) e injeta.
# Ainda nao usado em producao real nesta fase (bucket real, credencial
# real e ativacao continuam fora do escopo desta implementacao local).
boto3==1.43.92
