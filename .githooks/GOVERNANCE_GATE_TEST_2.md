# Teste temporário — Governance Gate V1

Arquivo inofensivo criado exclusivamente para validar que uma alteração em caminho crítico (`.githooks/`) exige aprovação humana externa ao diff.

Não contém código, não altera hooks e não deve ser mesclado em `main`.
